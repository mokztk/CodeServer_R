services:
  code_server:
    build:
      context: .
      dockerfile: Dockerfile
    image: "mokztk/r_codeserver:4.5.1"
    container_name: "codeserver"
    ports:
      - "8080:8080"
      - "8088:8088"
    volumes:
      - ./settings.json:/home/coder/.local/share/code-server/User/settings.json
